@layout({ title: movie.title })
  @slot('meta')
    <meta name="description" content="{{ movie.summary }}" />
  @endslot

  <h1>{{ movie.title }}</h1>

  @if (movie.abstract)
    <div class="p-8 my-8 bg-slate-100 rounded-xl">
      {{{ movie.abstract }}}
    </div>
  @else
    No Abstract
  @endif
@end
